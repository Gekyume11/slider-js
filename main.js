document.querySelector(".menu-btn").onclick=()=>{document.querySelector(".menu-btn").classList.toggle("active"),document.querySelector("menu").classList.toggle("active")};let slides=document.querySelectorAll(".slide"),counter=0;function updateSlides(){slides.forEach((e,t)=>e.style.transform=`translateX(${(t-counter)*100}%)`)}function next(){counter>=slides.length-1?counter=0:counter++,updateSlides()}function prev(){counter<=0?counter=slides.length-1:counter--,updateSlides()}slides.forEach((e,t)=>e.style.transform=`translateX(${100*t}%)`),setInterval(next,6e3);